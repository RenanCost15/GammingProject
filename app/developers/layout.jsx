export const metadata = {
  title: 'Desenvolvedoras | RedCore Games',
  description: 'Seção de desenvolvedoras do RedCore Games.',
};

export default function SectionLayout({ children }) {
  return children;
}
