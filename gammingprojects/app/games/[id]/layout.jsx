export const metadata = {
  title: 'Detalhes do jogo | RedCore Games',
  description: 'Informações detalhadas de jogos no RedCore Games.',
};

export default function SectionLayout({ children }) {
  return children;
}
